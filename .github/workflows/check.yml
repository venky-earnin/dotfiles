name: Check

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate repo
        run: ./scripts/check.sh
      - name: Smoke test bootstrap
        run: ./scripts/smoke-clean-home.sh
