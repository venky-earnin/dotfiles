# Local-first shell history. Run `atuin login` and enable sync only if you want encrypted cross-machine history.
auto_sync = false
update_check = false
search_mode = "fuzzy"
filter_mode = "global"
style = "compact"
inline_height = 20
enter_accept = true

[ai]
enabled = true
